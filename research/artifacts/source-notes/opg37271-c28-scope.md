# C28 source, scope and reproduction note

Verdict: candidate_only. Best verified result: none.
Input main: 69b475b0ce24540775d0f0149d446212415146b0.
Only vibemathing/problem-opg-37271-star-chromatic-index-cubic was operated on.

The exact target is Section4 of the main C27 direct notes: one t self row,
two coherent t-p rows, three coherent t-q rows and one exterior p-q equality,
with the specified new profile. C28 realizes this exact multiplicity pattern
on an actual simple cubic graph. Unlike the earlier C27 core-incident example,
its exterior witness is disjoint from the support and connects TWO different
outside U components. The old frame is phase-optimal, NOT globally optimal.
Its unique one-row core is minimal within the fixed frame only.

The new saturation proof applies to arbitrary legal completions of the
explicit colored motif, not to every realization of the same algebraic rows.
It needs no unused global A color and no hypothesis about single-edge paths.
Other geometric realizations under global and secondary extremality remain
open. This is the exact missing GLOBAL-EXCHANGE gate, not a transport defect.

Minimum order ten is for EXACT old witness occurrences and three U variables.
The productive-D-edge reduction is for that geometric order lower bound only;
it is not legitimate for preserving the endpoint family or global optimality.
The count12,976 in the proof is the SUM over the sixteen small cases in EACH
implementation; it is not the sum of both implementations or an all-graph
census. Global A-label permutations and all four coherent sign gauges are
accounted for. No previous order-ten full-frame enumeration was rerun.

The producer and literal checker import neither each other nor C20-C27 code.
The producer uses four-edge subsets and least-vertex matching enumeration;
the checker uses injective vertex walks, literal six-color support fillings
and increasing disjoint-edge matching enumeration. They have the same generator
trust domain, so algorithmic diversity does not change the assurance role.
Twenty mutations are diagnostic invocations of altered inputs, claims or
representations; they are not twenty modified full-program builds.

The compact certificate binding retains the exact seven witnesses, h/r/g,
endpoint-family counts, sixteen lower-order case results and the full expanded
output hash. The expanded389493-byte endpoint/color/witness table regenerates
from the committed input and producer; it is supplied in the recovery archive
but is NOT asserted to be a main file. No hidden input is needed. Run in a
writable disposable checkout because the bounded runner creates outputs.

The committed execution record is the actual fixed final run of both sources,
not a rereading of an older result. CPython3.13.5, one thread; wall35s,
CPU30/31s,768MiB, file1MiB, stdout64KiB, stderr16KiB per child; both exit0.
A-color4 is also tested on a remote edge before the local motif recoloring,
and all24 global A-name permutations are checked.

C25, C26 and C27 remain unchanged. C24 PR37 is a separate older transport tail;
its status is not resolved by this C28 transaction. The two historical C26
packages retain their distinct identities. No EvidenceLink, records, schema,
Harness, workflow, verifier, Result or Solution was modified. Exact local
admission and trusted mathematical receipts remain pending.
