# C25 scope, provenance and retained outputs

Verdict: candidate_only. Input main: b66703bd76d56f0791ad58542ec8bc7d3b8ee35b.
Only repository vibemathing/problem-opg-37271-star-chromatic-index-cubic is used.

Root quantifiers are frozen by the canonical ProblemContract. The current
C25 target is global OUTER, not the legacy zero-edit leaf-extension assertion.
The paper proof and two new enumeration implementations are authored for this
candidate. No C20/C23R/C24 core source is imported. No external graph catalogue,
nauty, SMT, Lean, or external theorem is used in the new mathematical argument.

The C24 final attachment was read and all50 manifest entries were checked.
Its ZIP SHA-256 is ed128d70032e9d5c7f11a44730d0de636a0aaf16a152d59c6f607a0c28840625.
It is NOT identical to the older package on PR37. Fresh reads found PR37 open
on the C23 branch, head ba8a412e3fc7c9dc9c7954280bead8a6f1558016. Workflow34144839209
failed only its diff check101814494776 because mutations[0..19] used the reserved
JSON key outcome in that certificate. Packet101814495093 and snapshot101814495053
passed. No duplicate C24 files are transported in C25 and no old execution is
reported as a new run. The old transaction remains separate.

C23R comment5571265211 records a conditional isolated-color erasure rule.
C24's no-self-loop state is only a negative control/input for the NEW fork
exchange; its closed-SCC proof is not rerun or submitted as new mathematics.
The complete perfect-matching-U failure on graph18 was previously proposed in
C22 comment5568227902; C25 supplies the global census and a uniform folded-five
argument. It is not treated as a novel root counterexample.

The global and replay programs independently generate the complete frame/phase
transcript and SHA-256 it online. No complete transcript file is claimed stored.
They also emit per-U tables. Compact summary.json and run-receipt.json bind every
per-graph output hash, count and ordered frame/phase hash. The nineteen large
space10 output JSONs are deterministic reproduction products; optional copies
are included in the recovery archive, not claimed to be separate main inputs.
The graph catalogue,19 actual positive colorings,6 matching folds,full patch
profiles,mutations,all algorithm sources and actual final-stage receipt are
included as fixed candidate inputs. Reproduction needs no omitted tables.

The receipt is a compact export of actual final-stage observations. Read its
stage_fields to decode each row. The full original execution file is retained
in the recovery archive at its recorded digest; it is not a separate main input.
Timestamped regenerated execution bytes differ; the mathematical outputs and
stream hashes must agree. No raw private environment data,
credentials,hidden reasoning or entire conversation is stored.

Root OUTER, faithful formalization, trusted verification and closure remain
open. Finite G10 does not imply an arbitrary-order theorem or all subcubic graphs
of order<=10. No EvidenceLink, Result, Solution or truth record is written.
