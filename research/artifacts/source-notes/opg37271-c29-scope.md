# C29 scope and provenance

Verdict: candidate_only. Best verified result: none.
Only vibemathing/problem-opg-37271-star-chromatic-index-cubic is in scope.
Fresh main: d85f00bf7793de78a7d85e7d32b692925e8c2236; unique research Issue6.

The admitted root/leaf records and unchanged ProblemContract were freshly read.
C25 profile, C26 envelope and C27 fixed-endpoint absorption ceiling remain prior
candidates, not trusted results. C28's fixed source/proof blobs were matched to
the supplied archive using Git blob SHA1 and file SHA256. Its proof SHA256 is
74aba38c23bb212ccd32cfbbbd75e6e52adaef04279387f1614bb8f3154a92b4.
No old mathematical output was reported as a fresh run.

A discovery-only probe of C28's productive-matching method found another exact
seven-row embedding. A broader passive-completion discovery scan hit its35s
limit. Its partial aggregate counts are NOT claimed as a complete census or
used for the proof. The final fixed C29 input has every edge explicitly listed;
the final producer/checker import no old candidate code or each other.
The finite family is exactly125 possible passive-color triples on48,49,89
around a specified thirteen-edge embedding, of which6 satisfy every premise.
Each resulting complete graph is checked with ALL its passive edges retained.
This is not deletion of constraints from a fixed-frame argument, and it is not
a repeated ten-vertex all-frame census.

The main graph is connected simple subcubic on10 vertices, with one degree-one
vertex; it is not asserted cubic. It realizes the exact old signed pattern and
has an explicit better frame, hence is not globally frame-optimal. All9 closed
singleton supports and11 closed connected two-edge supports are covered; only
the latter contain improving endpoints. The full core-hull families (25 and147
endpoints) also include improvements. No all-family failure is asserted.

The arbitrary-completion theorem is for one explicit nine-vertex saturated
motif, with all four A colors including a passive color4 blocker. The joint
replacement02:1->4 and08:2->3 eliminates an unavoidable self witness without
creating any new actual forbidden shape. Every old phase, including arbitrary
outside phases, improves. A possible third U edge at6 is explicitly allowed
under the preframe length limit. This excludes this motif from every global
optimum regardless of secondary extrema. Other embeddings are still open.

The committed summary retains the complete minimal-support endpoint entry
and phase-cost tables, the six passive cases, exact h/r/g rows and all hashes.
The fuller246751-byte actual-witness certificate regenerates from committed
source and input and is retained in the recovery archive. It is an OUTPUT,
not a hidden input and not falsely said to be a main file. The checker rebuilds
its full word/phase/witness data, rather than accepting hash agreement alone.

Execution is local, bounded, exact standard-library CPU work in the generator
trust domain. It is not remote repository command execution or a registered
trusted verifier run. No EvidenceLink, Result, Solution, records, Harness,
schema, workflow or verifier file is changed. Prior C24 PR37 and alternative
C26 artifact versions are separate historical transactions, not overwritten.

First open obligation: classify other witness embeddings and complete passive
D constraints under a hypothetical positive global optimum and the specified
secondary minimal core/support. Neither a complete embedding classification
nor universal availability of the joint repair is claimed.
