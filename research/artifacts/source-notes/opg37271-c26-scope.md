# C26 scope and source-faithfulness note

Verdict: candidate_only. Best verified result: none.
Fresh input main: bf3b3306434831fd5300670fb6a2de92914787f2.
Only repository vibemathing/problem-opg-37271-star-chromatic-index-cubic is used.

C25 PR38 is already merged. Its full boundary PROFILE criterion and pointed
sufficient condition remain unchanged. The current proof blob matches
918968a5e8c08b2434105d1b4c245250d26b99d3. The local C25 recovery archive's nineteen
manifest entries were checked against actual bytes, including the main proof.
Old C25 nineteen-type/global enumeration results are NOT new execution here.
Graph18's perfect-matching-only failure is retained as a known restriction,
not a route retried positively. C24's graph is reused with a different word.

Fresh Issue6 checkpoint5583313383 and C23R comment5571265211 explain the preceding
scope. C24 PR37 remains open at ba8a412e3fc7c9dc9c7954280bead8a6f1558016 on the old
C23 branch. Its old reserved JSON key failure and stale base are a separate
transport tail; this PR neither overwrites it nor transports duplicate C24 data.
The failed-route ledger is presently empty, so older candidate failure proposals
are consulted explicitly and not silently treated as admitted records.

New mathematics is self-contained: shortest-cycle splitting, exact row counting,
finite min-sum factorization, actual graph witnesses and finite enumeration.
No signed-subcubic theorem is applied to the phase graph: its maximum degree is
not constrained to three by cubicity of the original graph. Background signed
frustration terminology is compared only with Sivaraman, arXiv:1403.7212, and
Chen--Li--Wang, arXiv:2511.15226. Neither paper is a proof dependency or a novelty
certificate. No external graph catalogue or mathematical code is imported.

Pressure testing corrected a proposed closure illustration: edge-singleton
matching components do NOT propagate by vertex overlap. The final proof uses
overlapping THREE-edge paths instead; q1..8 are graph-checked. An early C24
numeric-word decoding probe had its orientation corrected before the explicit
C26 input was frozen. No prototype counts appear in the final certificate.

The central limitation is mathematical, not a transport issue. The fixed
example is not globally minimum over frames: a one-D-edge change lowers mu to0
by changing the optimum exterior phase. Its width4 obstruction concerns ONLY
pointed improvement at old attaining boundaries. It cannot exclude small
unrestricted exchanges or exchanges from hypothetical positive GLOBAL optima.
C25's pointed criterion was sufficient, not necessary, so it is not contradicted.

All three smaller-graph catalogues and per-state improvement endpoints are
regenerated by the committed runner, checked with another implementation and
bound by output hashes in summary. The combined114240-byte table is retained
in the recovery archive, not required as a hidden input or claimed a main file.
The full116-interface table on the fixed eight-vertex frame IS committed.
The runner needs only the new source/input files; it regenerates all results.

No actual Lean/Lake executable, faithful formal source, matching admitted local
obligation, allowlisted mathematical run, semantic receipt or closure exists.
The request records exact missing gates without changing the Web trust role.
Neither the candidate checks nor this PR close ROOT or the leaf obligation.
