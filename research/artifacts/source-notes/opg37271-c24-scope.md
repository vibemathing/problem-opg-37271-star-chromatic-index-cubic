# C24 provenance, scope and transport recovery

Verdict: candidate_only. Single repository only.
Fresh input main b66703bd76d56f0791ad58542ec8bc7d3b8ee35b; Issue6 comment5571265211
is the reread C23R recovery record. The existing C23 branch is at that main and its
C23 packet returned404. C23R is not assumed merged. Old unattached blobs and absent
C23 final checker/proof bytes are not inputs to the new C24 code.

C20 proof/core/admission request and C21 PRE/OUTER DAG are scope references at this
input revision. The exact original C19 certificate and frozen C19-E/C20 sources are
replayed as separately labeled regressions. They are not used as C24 generator code.
The new C++ component-counting implementation and Python graph-witness/partition
implementation are algorithmically different but share the candidate-generation
trust domain. No stronger verifier identity is assigned.

The previous C23R claim concerned strict one-entry descent and a conditional safe
core eraser. C24 negates the stronger attempted rescue: even a positive neutral
component can be closed. All D/U partitions and A-colorings remain in the domain;
all phases are counted. The explicit singleton excludes all48 outgoing entry edits.
This is not a claim that the entire frame space lacks a balanced state. The same
complete cubic graph has a separately checked global matching-U star-five coloring.
SQ, ODD and K3,3 are not substituted for a new universal construction.

Historical missing files remain missing and unused. The earlier prototype catalogue
had an invalid future-deficiency pruning condition and returned an empty domain;
that development result was rejected and the condition corrected before acceptance.
A full orchestration call was interrupted at the front-end boundary after14 successful
stages, without a final exit receipt; it is not the delivered execution. The final
resumable runner source was frozen, then completed the corpus in two bounded
foreground invocations. Its20 real stage receipts and all output hashes are delivered. The final audit also
checks every initial witness/phase field against the raw edge table; preliminary
audit output was superseded and regenerated after tightening those mutation checks.

All .states and .blocks tables are deterministic regenerated outputs, not hidden inputs.
The summary contains the graph edge lists, all closed members and stream digests.
The runner materializes every state, phase optimum, descent-parent certificate and
block exit. Full mathematical tables are also in the replay-output attachment; no
raw host diagnostics, credentials, private endpoints or chat transcripts are included.

First open mathematical node: OUTER. The universal EXIT route is a failed-route
PROPOSAL in the packet only; no ledger is changed. Next atomic action is a global
short-path frame selection or a controlled multi-edge/uphill exchange from a stated
starting family. Universal matching-only U is not silently reintroduced.
